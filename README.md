# zta
A cloud-based banking web application implementing Zero Trust Architecture. It uses JWT authentication, continuous verification, device validation, and least-privilege access to secure transactions, built using Node.js and Express.
